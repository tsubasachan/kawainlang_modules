name = "std/http"
version = "0.4.0"
description = "Cliente HTTP GET, parsers HTTP/URL, servidor HTTP simples e helpers de rota para KawaiLang"
entry = "http.🎀"
license = "MIT"

[ffi]
python = "http.py"
c = "http.c"

[files]
kawai = ["http.🎀", "server.🎀"]

[compat]
kawailang = ">=0.1.89"
